
// firebase-config.js
// Aquí se pegan las credenciales del proyecto Firebase.
// Este archivo queda listo para la siguiente fase de conexión real con base de datos.

const firebaseConfig = {
  apiKey: "PEGAR_API_KEY",
  authDomain: "PEGAR_AUTH_DOMAIN",
  projectId: "PEGAR_PROJECT_ID",
  storageBucket: "PEGAR_STORAGE_BUCKET",
  messagingSenderId: "PEGAR_MESSAGING_SENDER_ID",
  appId: "PEGAR_APP_ID"
};

// En la siguiente versión se conectará:
// firebase.initializeApp(firebaseConfig);
// const db = firebase.firestore();
